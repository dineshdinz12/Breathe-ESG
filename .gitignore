venv/
__pycache__/
*.pyc
*.pyo
.env
.env.local
*.log
.DS_Store
node_modules/

# Keep staticfiles in git so Railway can serve the built SPA
# (Railway doesn't run npm build in the Python buildpack)
# frontend/dist/ is not used — output goes to backend/staticfiles/
